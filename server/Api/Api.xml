<?xml version="1.0"?>
<doc>
    <assembly>
        <name>Api</name>
    </assembly>
    <members>
        <member name="M:BlocksTemplate.Api.Controllers.CertificateController.UploadCertificate(Microsoft.AspNetCore.Http.IFormFile,System.Boolean)">
            <summary>
            Stores a public certificate for the calling tenant and returns the URL to read it back.
            </summary>
            <remarks>
            The file arrives as multipart form data while the flag arrives on the query string, so
            both are bound explicitly - a single complex parameter would be inferred as
            <c>[FromBody]</c> and reject the upload. Callers also send a <c>TenantId</c> query
            parameter; it is ignored, because the tenant comes from the request context.
            </remarks>
        </member>
        <member name="T:BlocksTemplate.Api.Controllers.FunctionInvokeController">
            <summary>
            The public function endpoint (DECISIONS D5): <c>POST /api/fn/{functionId}</c>.
            <para>
            The tenant comes from <see cref="T:Blocks.Genesis.BlocksContext"/>, never from the route. Genesis'
            <c>TenantValidationMiddleware</c> guards every path under <c>api</c> — that prefix is in
            its default set, so this route is covered — resolving the tenant from the
            <c>x-blocks-key</c> header (or the query/form fallbacks in
            <c>TenantContextHelper.ResolveTenantIdFromHeaders</c>), validating it against the real
            tenant record, and calling <c>EnsureTenantContext</c> before any controller runs. So a
            caller must present a tenant key to reach this action at all, and that key is the only
            tenant identity the platform has actually verified.
            </para>
            <para>
            <b>Never take a tenant from the route, the query or the body.</b> The middleware has
            already authenticated one tenant — the header's — so a second, unvalidated tenant id in
            the path would let any valid <c>x-blocks-key</c> reach a <c>Public</c> function in
            whatever other tenant the caller could name, while every gate here was evaluated against
            that named tenant rather than the authenticated one. There is exactly one tenant in this
            request and it comes from <see cref="T:Blocks.Genesis.BlocksContext"/>.
            </para>
            <para>
            The invoke action itself carries no <c>[Authorize]</c>: a tenant key identifies a tenant
            but authenticates no user, and whether a caller may proceed is the function's own trigger
            configuration (public vs. token-protected), decided inside
            <c>FunctionInvocationService.InvokeHttpAsync</c> — this mirrors how the workflow webhook
            action is anonymous at the controller and authorizes itself internally per-trigger.
            </para>
            </summary>
        </member>
        <member name="M:BlocksTemplate.Api.Controllers.FunctionInvokeController.ShouldWait(System.Boolean)">
            <summary>
            <c>?wait=true</c>, matching DECISIONS D5. A <c>Prefer: wait=&lt;sec&gt;</c> header is
            also accepted for parity with the spec's Sync/Fire/Poll language, treated the same
            as a plain <c>wait=true</c> — the actual wait budget is the function's own timeout
            plus a fixed grace, not whatever value the header names.
            </summary>
        </member>
        <member name="T:BlocksTemplate.Api.Controllers.FunctionsController">
            <summary>
            The authenticated management surface: everything a tenant does to their own functions
            in the Blocks Studio. The public invocation endpoint lives separately in
            <see cref="T:BlocksTemplate.Api.Controllers.FunctionInvokeController"/> — it is reached by callers who are not
            necessarily Blocks users at all, and carries none of the assumptions this controller
            makes about an authenticated, already-authorized caller.
            </summary>
        </member>
        <member name="M:BlocksTemplate.Api.Controllers.LanguageController.Gets(Common.InternalService.Language.GetLanguagesRequest)">
            <summary>
            Retrieves all available languages.
            </summary>
            <returns>A list of <see cref="T:Common.InternalService.Language.Language"/> objects.</returns>
        </member>
        <member name="M:BlocksTemplate.Api.Controllers.SchedulerController.CreateSchedule(Scheduler.DomainService.Dtos.Requests.CreateScheduleRequestDto)">
            <summary>
            Creates a schedule.
            </summary>
        </member>
        <member name="M:BlocksTemplate.Api.Controllers.SchedulerController.UpdateSchedule(Scheduler.DomainService.Dtos.Requests.UpdateScheduleRequestDto)">
            <summary>
            Updates an existing schedule.
            </summary>
        </member>
        <member name="M:BlocksTemplate.Api.Controllers.SchedulerController.DeleteSchedule(Scheduler.DomainService.Dtos.Requests.DeleteScheduleRequestDto)">
            <summary>
            Permanently deletes a schedule.
            </summary>
        </member>
        <member name="M:BlocksTemplate.Api.Controllers.SchedulerController.GetSchedules(Scheduler.DomainService.Dtos.Requests.GetSchedulesRequestDto)">
            <summary>
            Get schedules.
            </summary>
        </member>
        <member name="T:BlocksTemplate.Api.GlobalApiRoutePrefixConvention">
            <summary>Prepends a segment (e.g. <c>api</c>) to every controller’s attribute route template.</summary>
        </member>
        <member name="M:BlocksTemplate.Api.GlobalApiRoutePrefixConvention.#ctor(System.String)">
            <summary>Prepends a segment (e.g. <c>api</c>) to every controller’s attribute route template.</summary>
        </member>
        <!-- Badly formed XML comment ignored for member "T:Utilities.Api.Controllers.ProxyController" -->
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyController.Test(Proxy.DomainService.Dtos.ProxyTestRequestDto)" -->
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyController.GetExecutions(Proxy.DomainService.Dtos.ProxyGetExecutionsRequestDto)" -->
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyController.GetExecution(Proxy.DomainService.Dtos.ProxyGetExecutionRequestDto)" -->
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyController.GetOverview(Proxy.DomainService.Dtos.ProxyGetOverviewRequestDto)" -->
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyController.ExportExecutionsCsv(Proxy.DomainService.Dtos.ProxyExportExecutionsRequestDto)" -->
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyController.Variables(Proxy.DomainService.Dtos.ProxyVariableListRequestDto)" -->
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyController.VariableTags" -->
        <member name="T:Utilities.Api.Controllers.ProxyGatewayController">
            <summary>
            Data-plane forwarder (Phase 2). A tenant's client calls
            <c>{METHOD} /api/proxy/gateway/{slug}/{**path}</c> instead of the vendor; Blocks authenticates the
            caller with the <c>X-Blocks-Key</c> tenant header + a bearer valid for that tenant, rebuilds the
            request against the stored upstream, attaches ONLY the configured headers / query params, calls the
            third party server-side, relays the response, and records one <see cref="T:Proxy.DomainService.Entities.ProxyExecutionEntity"/> per
            attempt.
            <para>
            The route is the literal <c>api/proxy/gateway</c> (NOT the <c>[controller]/[action]</c> convention);
            <see cref="T:BlocksTemplate.Api.GlobalApiRoutePrefixConvention"/> skips it so the effective path is
            exactly <c>/api/proxy/gateway/...</c> and never <c>/api/api/...</c>. It is <see cref="T:Microsoft.AspNetCore.Authorization.AllowAnonymousAttribute"/>
            at the framework level because the credential is the <c>X-Blocks-Key</c> header, not the standard
            scheme; auth is done here via <see cref="T:Proxy.DomainService.Services.IProxyGatewayAuthService"/>.
            </para>
            </summary>
        </member>
        <member name="M:Utilities.Api.Controllers.ProxyGatewayController.Relay(Proxy.DomainService.Services.ProxyForwardResult)">
            <summary>Relays the upstream status, body, and Content-Type byte-for-byte. No other header is relayed.</summary>
        </member>
        <!-- Badly formed XML comment ignored for member "M:Utilities.Api.Controllers.ProxyGatewayController.GatewayError(System.String,System.Int32,System.String,System.Nullable{System.Int32})" -->
        <member name="T:Program">
            <summary>
            Auto-generated public partial Program class for top-level statement apps.
            </summary>
        </member>
    </members>
</doc>
