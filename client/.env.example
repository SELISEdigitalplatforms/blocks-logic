# Sent as `x-blocks-key` on all `/api/*` requests from the SPA.
# Exposed via `envPrefix: ['BLOCKS_']` in vite.config (see `import.meta.env.BLOCKS_X_BLOCKS_KEY`).
# Copy to `.env` and set a value. Restart `npm run dev` / `dotnet run` after changes.
BLOCKS_X_BLOCKS_KEY=
